# --- Core server ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# --- LLM clients ---
openai==1.47.0
transformers==4.44.2

# --- Inference / ML runtimes ---
torch==2.3.1         # CPU build; avoid GPU in Cloud Run unless needed
onnxruntime==1.19.2
numpy==1.26.4

# --- Embeddings / RAG ---
sentence-transformers==3.1.1

# --- Networking (compat with openai 1.47.0) ---
httpx==0.27.2
